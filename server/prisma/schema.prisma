generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url      = env("DATABASE_URL")
}

model login {
    login_id  Int    @id @default(autoincrement())
    username  String
    password  String
    reg_email String
}
